---
phase: 13-status-processing-and-matching
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/monitoring/mismatch_detector.py
  - backend/app/workers/source_toggle.py
  - backend/app/workers/poll_prophetx.py
  - backend/app/workers/poll_sports_data.py
  - backend/app/workers/poll_espn.py
  - backend/app/workers/poll_oddsblaze.py
  - backend/app/workers/poll_odds_api.py
  - backend/app/workers/ws_prophetx.py
  - backend/tests/test_mismatch_detector.py
autonomous: true
requirements: [MISM-01, AMQP-03]

must_haves:
  truths:
    - "compute_status_match returns False when opticodds_status disagrees with prophetx_status"
    - "compute_status_match returns True when opticodds_status is None (NULL-safe for non-tennis)"
    - "compute_is_critical counts opticodds live status toward the 2-source threshold"
    - "All 13 existing call sites pass event.opticodds_status as the 6th argument"
    - "source_toggle clear_source_and_recompute works for opticodds source"
  artifacts:
    - path: "backend/app/monitoring/mismatch_detector.py"
      provides: "_OPTICODDS_CANONICAL dict + 6-param compute_status_match + 6-param compute_is_critical"
      contains: "_OPTICODDS_CANONICAL"
    - path: "backend/app/workers/source_toggle.py"
      provides: "opticodds entry in SOURCE_COLUMN_MAP"
      contains: '"opticodds": "opticodds_status"'
    - path: "backend/tests/test_mismatch_detector.py"
      provides: "OpticOdds-specific mismatch tests"
      contains: "TestComputeStatusMatchOpticOdds"
  key_links:
    - from: "backend/app/workers/poll_prophetx.py"
      to: "compute_status_match"
      via: "6th arg event.opticodds_status"
      pattern: "opticodds_status"
    - from: "backend/app/workers/ws_prophetx.py"
      to: "compute_status_match"
      via: "6th arg"
      pattern: "opticodds_status"
    - from: "backend/app/workers/source_toggle.py"
      to: "compute_status_match"
      via: "6th arg ev.opticodds_status"
      pattern: "opticodds_status"
---

<objective>
Extend the mismatch detector to include OpticOdds as a 6th source and update all call sites across the codebase.

Purpose: MISM-01 requires OpticOdds status to participate in mismatch detection for tennis events. The function signatures already accept 5 source params; this adds the 6th (opticodds_status) with a new canonical mapping dict. All 13 call sites must pass the actual event value instead of relying on the default None.

Output: Updated mismatch_detector.py with _OPTICODDS_CANONICAL + 6-param signatures, all worker call sites updated, source_toggle extended, new tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-status-processing-and-matching/13-CONTEXT.md
@.planning/phases/13-status-processing-and-matching/13-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/monitoring/mismatch_detector.py (CURRENT — will be modified):
```python
# Line 197-201 — existing canonical dict pattern to follow:
_ODDSBLAZE_CANONICAL: dict[str, str] = {
    "live": "inprogress",
    "scheduled": "scheduled",
    "final": "final",
}

# Line 204-210 — CURRENT 5-param signature (will become 6-param):
def compute_status_match(
    px_status: str | None,
    odds_api_status: str | None,
    sdio_status: str | None,
    espn_status: str | None = None,
    oddsblaze_status: str | None = None,
) -> bool:

# Line 239-244 — CURRENT 5-param signature (will become 6-param):
def compute_is_critical(
    px_status: str | None,
    odds_api_status: str | None,
    sdio_status: str | None,
    espn_status: str | None,
    oddsblaze_status: str | None = None,
) -> bool:
```

From backend/app/workers/source_toggle.py (lines 20-25):
```python
SOURCE_COLUMN_MAP = {
    "odds_api": "odds_api_status",
    "sports_data": "sdio_status",
    "espn": "espn_status",
    "oddsblaze": "oddsblaze_status",
}
```

From backend/app/models/event.py:
```python
opticodds_status = Column(String(50), nullable=True)  # Added Phase 12
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _OPTICODDS_CANONICAL dict and extend both function signatures in mismatch_detector.py + tests</name>
  <files>backend/app/monitoring/mismatch_detector.py, backend/tests/test_mismatch_detector.py</files>
  <read_first>
    - backend/app/monitoring/mismatch_detector.py
    - backend/tests/test_mismatch_detector.py
  </read_first>
  <behavior>
    - Test: compute_status_match("live", None, None, None, None, "in_progress") returns True (both map to inprogress)
    - Test: compute_status_match("live", None, None, None, None, "not_started") returns False (inprogress vs scheduled)
    - Test: compute_status_match("live", None, None, None, None, None) returns True (NULL-safe skip)
    - Test: compute_status_match("live", None, None, None, None, "walkover") returns False (inprogress vs final)
    - Test: compute_is_critical("not_started", None, None, None, None, "in_progress") returns False (only 1 live source)
    - Test: compute_is_critical("not_started", None, "inprogress", None, None, "in_progress") returns True (2 live sources)
    - Test: compute_is_critical("not_started", None, "inprogress", None, None, None) returns False (only 1 live, opticodds None skipped)
  </behavior>
  <action>
    Per D-09 and D-10 from CONTEXT.md:

    1. In `mismatch_detector.py`, add `_OPTICODDS_CANONICAL` dict after `_ODDSBLAZE_CANONICAL` (around line 201). This dict uses `scheduled/inprogress/final` namespace (NOT `not_started/live/ended` — that is the consumer's version). The exact dict:
    ```python
    _OPTICODDS_CANONICAL: dict[str, str] = {
        "not_started":   "scheduled",
        "scheduled":     "scheduled",
        "delayed":       "scheduled",
        "start_delayed": "scheduled",
        "postponed":     "scheduled",
        "in_progress":   "inprogress",
        "live":          "inprogress",
        "suspended":     "inprogress",
        "interrupted":   "inprogress",
        "finished":      "final",
        "complete":      "final",
        "retired":       "final",
        "walkover":      "final",
        "cancelled":     "final",
        "abandoned":     "final",
    }
    ```

    2. Add `opticodds_status: str | None = None` as the 6th parameter to `compute_status_match()` after `oddsblaze_status`. Add `(opticodds_status, _OPTICODDS_CANONICAL)` to the `sources` list inside the function.

    3. Add `opticodds_status: str | None = None` as the 6th parameter to `compute_is_critical()` after `oddsblaze_status`. Add `(opticodds_status, _OPTICODDS_CANONICAL)` to the `sources` list inside the function.

    4. In `test_mismatch_detector.py`, add a new test class `TestComputeStatusMatchOpticOdds` with the behavior tests listed above. Follow the existing test pattern in the file.

    IMPORTANT: The raw column values written by the consumer are `not_started/live/ended/walkover/retired/suspended`. But `compute_status_match` receives these raw values and must map them through `_OPTICODDS_CANONICAL` to `scheduled/inprogress/final` for comparison. The dict must handle BOTH the consumer's canonical outputs (not_started/live/ended) AND raw OpticOdds values (in_progress/finished/etc.) because D-06 says special statuses (walkover/retired/suspended) are written verbatim as the raw value.
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash/backend && python -m pytest tests/test_mismatch_detector.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - mismatch_detector.py contains `_OPTICODDS_CANONICAL: dict[str, str]` with at least 15 entries
    - mismatch_detector.py contains `def compute_status_match(` with `opticodds_status: str | None = None` parameter
    - mismatch_detector.py contains `def compute_is_critical(` with `opticodds_status: str | None = None` parameter
    - mismatch_detector.py contains `(opticodds_status, _OPTICODDS_CANONICAL)` in both functions' sources lists
    - test_mismatch_detector.py contains `class TestComputeStatusMatchOpticOdds`
    - test_mismatch_detector.py contains at least 5 test methods in that class
    - `cd backend && python -m pytest tests/test_mismatch_detector.py -x -q` exits 0
  </acceptance_criteria>
  <done>_OPTICODDS_CANONICAL dict exists in mismatch_detector.py with scheduled/inprogress/final values. Both functions accept 6 source params. Tests pass proving OpticOdds disagree returns False, None is skipped, and is_critical counts OpticOdds toward threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Update all compute_status_match call sites + source_toggle</name>
  <files>backend/app/workers/poll_prophetx.py, backend/app/workers/poll_sports_data.py, backend/app/workers/poll_espn.py, backend/app/workers/poll_oddsblaze.py, backend/app/workers/poll_odds_api.py, backend/app/workers/ws_prophetx.py, backend/app/workers/source_toggle.py</files>
  <read_first>
    - backend/app/workers/poll_prophetx.py
    - backend/app/workers/ws_prophetx.py
    - backend/app/workers/poll_sports_data.py
    - backend/app/workers/poll_espn.py
    - backend/app/workers/poll_oddsblaze.py
    - backend/app/workers/poll_odds_api.py
    - backend/app/workers/source_toggle.py
  </read_first>
  <action>
    Per D-11 from CONTEXT.md — all callers must pass `opticodds_status` as the 6th argument.

    **poll_prophetx.py** — 5 call sites:
    - Line ~207 (new event creation): change `compute_status_match(status_value, None, None, None, None)` to `compute_status_match(status_value, None, None, None, None, None)` — new events have no opticodds data yet
    - Line ~235 (existing event non-authoritative): add `existing.opticodds_status` as 6th arg after `existing.oddsblaze_status`
    - Line ~253 (existing event WS-authoritative recompute): add `existing.opticodds_status` as 6th arg
    - Line ~298 (stale event marked ended): add `event.opticodds_status` as 6th arg
    - Line ~314 (full recompute pass): add `event.opticodds_status` or the equivalent variable as 6th arg

    **poll_sports_data.py** — 1 call site:
    - Line ~648: add `px_event.opticodds_status` as 6th arg after `px_event.oddsblaze_status`

    **poll_espn.py** — 1 call site:
    - Line ~291: add the event object's `.opticodds_status` as 6th arg (check the variable name — it may be `best_match.opticodds_status` or `existing.opticodds_status`)

    **poll_oddsblaze.py** — 1 call site:
    - Line ~273: add `best_match.opticodds_status` as 6th arg. Note: `derived_status` is the 5th arg (oddsblaze_status) — the 6th is `best_match.opticodds_status`

    **poll_odds_api.py** — 1 call site:
    - Line ~254: add the event object's `.opticodds_status` as 6th arg

    **ws_prophetx.py** — 3 call sites:
    - Line ~155 (event deleted → marked ended): add `existing.opticodds_status` as 6th arg
    - Line ~221 (new event creation): change to pass `None` as 6th arg (new events have no opticodds data)
    - Line ~248 (existing event update): add `existing.opticodds_status` as 6th arg

    **source_toggle.py** — 2 changes:
    1. Add `"opticodds": "opticodds_status"` to `SOURCE_COLUMN_MAP` dict (line ~25)
    2. In `clear_source_and_recompute()` (line ~57), add `ev.opticodds_status if column_name != "opticodds_status" else None` as the 6th arg to `compute_status_match()`

    VERIFY after all changes: run `grep -n "compute_status_match(" backend/app/workers/*.py backend/app/workers/source_toggle.py` and confirm every call site has 6 arguments (or 6 `None`s for new events).
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash/backend && python -m pytest tests/test_mismatch_detector.py tests/test_opticodds_consumer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "opticodds_status" backend/app/workers/poll_prophetx.py` returns at least 5
    - `grep -c "opticodds_status" backend/app/workers/ws_prophetx.py` returns at least 3
    - `grep -c "opticodds_status" backend/app/workers/poll_sports_data.py` returns at least 1
    - `grep -c "opticodds_status" backend/app/workers/poll_espn.py` returns at least 1
    - `grep -c "opticodds_status" backend/app/workers/poll_oddsblaze.py` returns at least 1
    - `grep -c "opticodds_status" backend/app/workers/poll_odds_api.py` returns at least 1
    - source_toggle.py contains `"opticodds": "opticodds_status"`
    - source_toggle.py contains `ev.opticodds_status` in the compute_status_match call
    - `cd backend && python -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>All 13 compute_status_match call sites pass opticodds_status as the 6th argument. source_toggle.py SOURCE_COLUMN_MAP includes opticodds. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest tests/test_mismatch_detector.py -x -q` — all tests pass including new OpticOdds tests
2. `cd backend && python -m pytest tests/ -x -q` — full suite green (no regressions from call site updates)
3. `grep -rn "compute_status_match(" backend/app/workers/ backend/app/monitoring/ | grep -v "def compute_status_match" | grep -v "#"` — every call site passes 6 args
</verification>

<success_criteria>
- _OPTICODDS_CANONICAL dict in mismatch_detector.py with 15 entries using scheduled/inprogress/final namespace
- compute_status_match and compute_is_critical both accept 6 source params
- All 13 call sites updated to pass event.opticodds_status
- SOURCE_COLUMN_MAP includes opticodds → opticodds_status
- Full test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/13-status-processing-and-matching/13-01-SUMMARY.md`
</output>
