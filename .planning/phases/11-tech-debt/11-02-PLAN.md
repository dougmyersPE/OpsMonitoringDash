---
phase: 11-tech-debt
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/src/api/events.ts
  - frontend/src/api/usage.ts
  - frontend/src/components/EventsTable.tsx
  - frontend/src/components/SystemHealth.tsx
  - frontend/src/components/usage/SportsApiQuotaCard.tsx
  - frontend/src/components/usage/QuotaSection.tsx
  - frontend/src/components/usage/CallVolumeChart.tsx
  - frontend/src/components/usage/IntervalSection.tsx
  - frontend/src/components/usage/SourceToggleSection.tsx
  - frontend/src/components/usage/ProjectionCard.tsx
  - docs/architecture.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Events table does not show a Sports API column"
    - "System health page does not show a Sports API worker badge"
    - "API Usage page has no Sports API quota card, interval, toggle, chart line, or projection"
    - "ROADMAP and REQUIREMENTS reflect removal instead of refactor"
  artifacts:
    - path: "frontend/src/components/EventsTable.tsx"
      provides: "Events table without Sports API column"
    - path: "frontend/src/components/SystemHealth.tsx"
      provides: "Health badges without Sports API worker"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 11 goal text"
  key_links:
    - from: "frontend/src/api/events.ts"
      to: "frontend/src/components/EventsTable.tsx"
      via: "EventRow type"
      pattern: "sports_api_status should not appear in type"
---

<objective>
Remove all Sports API references from the frontend (events table, health badges, API usage page) and update planning docs (ROADMAP, REQUIREMENTS) to reflect removal instead of refactor.

Purpose: Complete the Sports API removal (D-01/D-02) by cleaning the frontend and keeping docs honest (D-04).
Output: Clean frontend with no Sports API references; updated planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tech-debt/11-CONTEXT.md
@.planning/phases/11-tech-debt/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Sports API from all frontend files</name>
  <files>
    frontend/src/api/events.ts
    frontend/src/api/usage.ts
    frontend/src/components/EventsTable.tsx
    frontend/src/components/SystemHealth.tsx
    frontend/src/components/usage/SportsApiQuotaCard.tsx
    frontend/src/components/usage/QuotaSection.tsx
    frontend/src/components/usage/CallVolumeChart.tsx
    frontend/src/components/usage/IntervalSection.tsx
    frontend/src/components/usage/SourceToggleSection.tsx
    frontend/src/components/usage/ProjectionCard.tsx
  </files>
  <read_first>
    frontend/src/api/events.ts
    frontend/src/api/usage.ts
    frontend/src/components/EventsTable.tsx
    frontend/src/components/SystemHealth.tsx
    frontend/src/components/usage/SportsApiQuotaCard.tsx
    frontend/src/components/usage/QuotaSection.tsx
    frontend/src/components/usage/CallVolumeChart.tsx
    frontend/src/components/usage/IntervalSection.tsx
    frontend/src/components/usage/SourceToggleSection.tsx
    frontend/src/components/usage/ProjectionCard.tsx
  </read_first>
  <action>
Per D-01 (full removal):

1. **`frontend/src/api/events.ts`**: Remove `sports_api_status: string | null;` from the EventRow interface.

2. **`frontend/src/api/usage.ts`**: Remove `sports_api: Record<string, SportQuota>;` from the quota type. If the backend no longer returns `sports_api` in the quota object, the type must match.

3. **`frontend/src/components/EventsTable.tsx`**:
   - Remove `"sports_api_status"` from the SortCol union type (line ~222).
   - Remove `"sports_api_status"` from the source columns array (line ~233).
   - Remove the `<SortableHead col="sports_api_status" ...>Sports API</SortableHead>` header cell (line ~510).
   - Remove the `<TableCell><SourceStatus status={event.sports_api_status} /></TableCell>` body cell (line ~559).

4. **`frontend/src/components/SystemHealth.tsx`**:
   - Remove `poll_sports_api: boolean;` from the WorkerHealth interface (line ~16).
   - Remove `{ key: "poll_sports_api", label: "Sports API" }` from the workers array (line ~30).

5. **DELETE** `frontend/src/components/usage/SportsApiQuotaCard.tsx` entirely.

6. **`frontend/src/components/usage/QuotaSection.tsx`**:
   - Remove the `import SportsApiQuotaCard from "./SportsApiQuotaCard";` import.
   - Remove `sports_api: Record<string, SportQuota>;` from the quota prop type.
   - Remove the `<SportsApiQuotaCard quota={quota.sports_api} />` JSX element.

7. **`frontend/src/components/usage/CallVolumeChart.tsx`**:
   - Remove `poll_sports_api: "#3b82f6"` from the colors map (line ~18).
   - Remove `poll_sports_api: "Sports API"` from the labels map (line ~26).

8. **`frontend/src/components/usage/IntervalSection.tsx`**:
   - Remove `poll_sports_api: "Sports API"` from the label map (line ~11).
   - Remove `poll_sports_api: "poll_interval_sports_api"` from the config key map (line ~20).

9. **`frontend/src/components/usage/SourceToggleSection.tsx`**:
   - Remove `sports_api: "Sports API"` from the source labels map (line ~8).

10. **`frontend/src/components/usage/ProjectionCard.tsx`**:
    - Remove `poll_sports_api: "Sports API"` from the label map (line ~7).
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash && ! grep -r "sports_api" frontend/src/ --include="*.ts" --include="*.tsx" -l && test ! -f frontend/src/components/usage/SportsApiQuotaCard.tsx && cd frontend && npx tsc --noEmit 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "sports_api" frontend/src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - frontend/src/components/usage/SportsApiQuotaCard.tsx does not exist
    - `npx tsc --noEmit` in frontend/ exits 0 (no TypeScript errors)
    - EventsTable.tsx does NOT contain string "Sports API" as a column header
    - SystemHealth.tsx does NOT contain "poll_sports_api"
  </acceptance_criteria>
  <done>All frontend files purged of Sports API references; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Update docs — ROADMAP, REQUIREMENTS, architecture</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    docs/architecture.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    docs/architecture.md
  </read_first>
  <action>
Per D-04 (keep docs honest):

1. **`.planning/ROADMAP.md`** Phase 11 section:
   - Update Goal from "SportsApiClient uses BaseAPIClient consistently, eliminating the architectural inconsistency" to: "Sports API integration fully removed — client, worker, DB column, and all references eliminated"
   - Update Success Criteria item 1 from "SportsApiClient inherits from BaseAPIClient..." to: "No Sports API client, worker, config, or DB column exists in the codebase"
   - Update Success Criteria item 2 from "Sports API poll worker behavior is unchanged..." to: "All other poll workers and mismatch detection continue working with remaining sources (Odds API, SDIO, ESPN, OddsBlaze)"
   - Update Success Criteria item 3 from "No regression in Sports API quota display..." to: "API Usage page displays only active sources; no Sports API quota/interval/toggle remains"
   - Update Plans count: `**Plans:** 2 plans`
   - Add plan list:
     ```
     Plans:
     - [ ] 11-01-PLAN.md — Remove Sports API backend (client, worker, migration, mismatch, config, tests)
     - [ ] 11-02-PLAN.md — Remove Sports API frontend + update docs
     ```

2. **`.planning/REQUIREMENTS.md`**:
   - Change DEBT-01 text from "SportsApiClient refactored to use BaseAPIClient pattern consistently" to: "Sports API integration fully removed (client, worker, DB column, config, frontend references)"

3. **`docs/architecture.md`**:
   - Find the sentence referencing `sports_api_status` in the Event description and remove `sports_api_status` from the list of source columns. The updated text should list: `prophetx_status`, `sdio_status`, `odds_api_status`, `espn_status`, `oddsblaze_status`.
   - Search for any other mentions of "Sports API", "api-sports.io", or "poll_sports_api" in the file and remove them.
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash && grep -q "fully removed" .planning/ROADMAP.md && grep -q "fully removed" .planning/REQUIREMENTS.md && ! grep -q "sports_api_status" docs/architecture.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md Phase 11 Goal contains "fully removed"
    - .planning/ROADMAP.md Phase 11 Plans shows 2 plans
    - .planning/REQUIREMENTS.md DEBT-01 contains "fully removed"
    - docs/architecture.md does NOT contain "sports_api_status"
  </acceptance_criteria>
  <done>Planning docs and architecture doc updated to reflect removal instead of refactor</done>
</task>

</tasks>

<verification>
1. `grep -r "sports_api" frontend/src/ --include="*.ts" --include="*.tsx"` returns empty
2. `npx tsc --noEmit` in frontend/ passes
3. ROADMAP Phase 11 goal says "removed" not "refactored"
4. REQUIREMENTS DEBT-01 says "removed" not "refactored"
5. docs/architecture.md has no sports_api_status mention
</verification>

<success_criteria>
- Zero occurrences of "sports_api" in frontend/src/**/*.{ts,tsx}
- SportsApiQuotaCard.tsx deleted
- TypeScript compiles without errors
- ROADMAP and REQUIREMENTS updated with removal language
- architecture.md updated
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt/11-02-SUMMARY.md`
</output>
