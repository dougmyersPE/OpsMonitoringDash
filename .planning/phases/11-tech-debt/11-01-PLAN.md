---
phase: 11-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/009_drop_sports_api_status.py
  - backend/app/models/event.py
  - backend/app/schemas/event.py
  - backend/app/core/config.py
  - backend/app/monitoring/mismatch_detector.py
  - backend/app/workers/celery_app.py
  - backend/app/workers/beat_bootstrap.py
  - backend/app/workers/poll_sports_api.py
  - backend/app/clients/sports_api.py
  - backend/app/workers/source_toggle.py
  - backend/app/workers/rollup_api_usage.py
  - backend/app/api/v1/health.py
  - backend/app/api/v1/usage.py
  - backend/app/api/v1/events.py
  - backend/app/api/v1/config.py
  - backend/app/workers/poll_critical_check.py
  - backend/app/workers/ws_prophetx.py
  - backend/app/workers/poll_prophetx.py
  - backend/app/workers/poll_odds_api.py
  - backend/app/workers/poll_espn.py
  - backend/app/workers/poll_oddsblaze.py
  - backend/app/workers/poll_sports_data.py
  - backend/app/seed.py
  - backend/tests/test_health.py
  - backend/tests/test_status_authority.py
  - backend/tests/test_ws_upsert.py
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Sports API client and poll worker no longer exist in the codebase"
    - "Event model has no sports_api_status column"
    - "Mismatch detector does not reference Sports API in any function"
    - "Health endpoint does not check poll_sports_api heartbeat"
    - "Usage endpoint does not track Sports API quota or calls"
    - "Celery does not register or schedule poll_sports_api"
    - "All compute_status_match calls pass 4 source args (odds_api, sdio, espn, oddsblaze) not 5"
  artifacts:
    - path: "backend/alembic/versions/009_drop_sports_api_status.py"
      provides: "Alembic migration dropping sports_api_status column"
      contains: "op.drop_column"
    - path: "backend/app/monitoring/mismatch_detector.py"
      provides: "Mismatch detection without Sports API"
  key_links:
    - from: "backend/app/monitoring/mismatch_detector.py"
      to: "all poll workers"
      via: "compute_status_match signature change"
      pattern: "compute_status_match\\(.*px.*odds.*sdio.*espn.*oddsblaze"
---

<objective>
Remove all Sports API (api-sports.io) backend integration: client, worker, DB column, config, mismatch detector references, health checks, usage tracking, and update all callers of compute_status_match/compute_is_critical/compute_is_flagged.

Purpose: Eliminate redundant data source per D-01/D-02 — Sports API adds noise, not signal, and the remaining sources (SportsDataIO, ESPN, OddsBlaze, Odds API) provide sufficient coverage.
Output: Clean backend with no Sports API references; Alembic migration 009 to drop column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tech-debt/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Sports API files, create migration, update model/schema/config</name>
  <files>
    backend/app/clients/sports_api.py
    backend/app/workers/poll_sports_api.py
    backend/alembic/versions/009_drop_sports_api_status.py
    backend/app/models/event.py
    backend/app/schemas/event.py
    backend/app/core/config.py
  </files>
  <read_first>
    backend/app/clients/sports_api.py
    backend/app/workers/poll_sports_api.py
    backend/app/models/event.py
    backend/app/schemas/event.py
    backend/app/core/config.py
    backend/alembic/versions/008_add_status_authority_columns.py
  </read_first>
  <action>
Per D-01 (full removal) and D-02 (remove entirely, not refactor):

1. **DELETE** `backend/app/clients/sports_api.py` entirely.
2. **DELETE** `backend/app/workers/poll_sports_api.py` entirely.

3. **Create** `backend/alembic/versions/009_drop_sports_api_status.py`:
   - revision id: generate unique hex
   - down_revision: the revision id from 008_add_status_authority_columns.py
   - upgrade(): `op.drop_column('events', 'sports_api_status')`
   - downgrade(): `op.add_column('events', sa.Column('sports_api_status', sa.String(50), nullable=True))`

4. **Edit** `backend/app/models/event.py`:
   - Remove the line: `sports_api_status: Mapped[str | None] = mapped_column(String(50), nullable=True)`

5. **Edit** `backend/app/schemas/event.py`:
   - Remove `sports_api_status: str | None` from EventResponse
   - Update the `is_critical` computed_field call to remove `self.sports_api_status` arg:
     ```python
     return compute_is_critical(
         self.prophetx_status,
         self.odds_api_status,
         self.sdio_status,
         self.espn_status,
         self.oddsblaze_status,
     )
     ```

6. **Edit** `backend/app/core/config.py`:
   - Remove: `SPORTS_API_KEY: str | None = None`
   - Remove: `POLL_INTERVAL_SPORTS_API: int = 1800`
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash && test ! -f backend/app/clients/sports_api.py && test ! -f backend/app/workers/poll_sports_api.py && test -f backend/alembic/versions/009_drop_sports_api_status.py && ! grep -q "sports_api_status" backend/app/models/event.py && ! grep -q "sports_api_status" backend/app/schemas/event.py && ! grep -q "SPORTS_API_KEY" backend/app/core/config.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/clients/sports_api.py does not exist
    - backend/app/workers/poll_sports_api.py does not exist
    - backend/alembic/versions/009_drop_sports_api_status.py exists and contains `op.drop_column('events', 'sports_api_status')`
    - backend/app/models/event.py does NOT contain `sports_api_status`
    - backend/app/schemas/event.py does NOT contain `sports_api_status`
    - backend/app/core/config.py does NOT contain `SPORTS_API_KEY` or `POLL_INTERVAL_SPORTS_API`
  </acceptance_criteria>
  <done>Sports API client, worker, and DB column definition removed; migration 009 created to drop column</done>
</task>

<task type="auto">
  <name>Task 2: Update mismatch detector, all workers, health, usage, celery, config API, seed, and tests</name>
  <files>
    backend/app/monitoring/mismatch_detector.py
    backend/app/workers/celery_app.py
    backend/app/workers/beat_bootstrap.py
    backend/app/workers/source_toggle.py
    backend/app/workers/rollup_api_usage.py
    backend/app/workers/ws_prophetx.py
    backend/app/workers/poll_prophetx.py
    backend/app/workers/poll_odds_api.py
    backend/app/workers/poll_espn.py
    backend/app/workers/poll_oddsblaze.py
    backend/app/workers/poll_sports_data.py
    backend/app/workers/poll_critical_check.py
    backend/app/api/v1/health.py
    backend/app/api/v1/usage.py
    backend/app/api/v1/events.py
    backend/app/api/v1/config.py
    backend/app/seed.py
    backend/tests/test_health.py
    backend/tests/test_status_authority.py
    backend/tests/test_ws_upsert.py
  </files>
  <read_first>
    backend/app/monitoring/mismatch_detector.py
    backend/app/workers/celery_app.py
    backend/app/workers/beat_bootstrap.py
    backend/app/workers/source_toggle.py
    backend/app/workers/rollup_api_usage.py
    backend/app/api/v1/health.py
    backend/app/api/v1/usage.py
    backend/app/api/v1/events.py
    backend/app/api/v1/config.py
    backend/app/seed.py
    backend/app/workers/ws_prophetx.py
    backend/app/workers/poll_prophetx.py
    backend/app/workers/poll_odds_api.py
    backend/app/workers/poll_espn.py
    backend/app/workers/poll_oddsblaze.py
    backend/app/workers/poll_sports_data.py
    backend/app/workers/poll_critical_check.py
    backend/tests/test_health.py
    backend/tests/test_status_authority.py
    backend/tests/test_ws_upsert.py
  </read_first>
  <action>
Per D-01 (full removal), D-05 (Claude decides cleanup approach for match logic):

**A. Mismatch detector** (`backend/app/monitoring/mismatch_detector.py`):

1. Remove `_SPORTS_API_FLAG_STATUSES` set (lines 66-71).
2. Remove `_SPORTS_API_CANONICAL` dict (lines 218-254).
3. Remove `_sports_api_to_canonical()` function (lines 257-267).
4. Update `compute_is_flagged()` signature — remove `sports_api_status` param entirely. New signature:
   ```python
   def compute_is_flagged(sdio_status: str | None) -> bool:
       return bool(sdio_status and sdio_status in FLAG_ONLY_STATUSES)
   ```
5. Update `compute_status_match()` — remove `sports_api_status` parameter. New signature:
   ```python
   def compute_status_match(
       px_status: str | None,
       odds_api_status: str | None,
       sdio_status: str | None,
       espn_status: str | None = None,
       oddsblaze_status: str | None = None,
   ) -> bool:
   ```
   In the `sources` list inside, remove the `(sports_api_status, None)` tuple. The remaining sources use their canonical dicts directly — no special-case function needed.

6. Update `compute_is_critical()` — remove `sports_api_status` parameter. New signature:
   ```python
   def compute_is_critical(
       px_status: str | None,
       odds_api_status: str | None,
       sdio_status: str | None,
       espn_status: str | None,
       oddsblaze_status: str | None = None,
   ) -> bool:
   ```
   Remove `(sports_api_status, None)` from the `sources` list inside.

**B. Celery app** (`backend/app/workers/celery_app.py`):
- Remove `"app.workers.poll_sports_api"` from the `include` list.

**C. Beat bootstrap** (`backend/app/workers/beat_bootstrap.py`):
- Remove `"sports_api": "app.workers.poll_sports_api.run"` from `WORKER_TASK_MAP`.
- Remove `"sports_api": "poll-sports-api"` from `BEAT_NAME_MAP`.
- Remove `"sports_api": 1800.0` from `_FALLBACK_INTERVALS`.

**D. Source toggle** (`backend/app/workers/source_toggle.py`):
- Remove `"sports_api": "sports_api_status"` from `SOURCE_COLUMN_MAP`.
- In `clear_source_and_recompute()`, update the `compute_status_match()` call to remove the `sports_api_status` arg. The call should pass:
  ```python
  ev.status_match = compute_status_match(
      ev.prophetx_status,
      ev.odds_api_status if column_name != "odds_api_status" else None,
      ev.sdio_status if column_name != "sdio_status" else None,
      ev.espn_status if column_name != "espn_status" else None,
      ev.oddsblaze_status if column_name != "oddsblaze_status" else None,
  )
  ```

**E. Rollup** (`backend/app/workers/rollup_api_usage.py`):
- Remove `"poll_sports_api"` from `WORKER_NAMES` list.

**F. Health endpoint** (`backend/app/api/v1/health.py`):
- Remove `"worker:heartbeat:poll_sports_api"` from the `keys` list.
- Remove `"poll_sports_api": results[3] is not None` from the return dict.
- Adjust index references: after removing index 3, `ws:connection_state` moves from results[5] to results[4], `ws:connection_state_since` from results[6] to results[5]. Update the `ws_state = results[...]` and `ws_since = results[...]` lines accordingly.

**G. Usage endpoint** (`backend/app/api/v1/usage.py`):
- Remove `"poll_sports_api"` from `WORKER_NAMES` list.
- Remove `SPORTS_API_FAMILIES` list entirely.
- Remove `"poll_sports_api"` entries from `INTERVAL_KEYS` dict.
- Remove the entire Sports API quota section (the for-loop over `SPORTS_API_FAMILIES` reading Redis keys).
- Remove `"sports_api": sports_api_quota` from the `quota` dict in the return statement.
- Remove `"sports_api"` from `source_toggle_keys` list.

**H. Events API** (`backend/app/api/v1/events.py`):
- Remove `from app.workers.poll_sports_api import run as poll_sports_api_task`.
- Remove `poll_sports_api_task.delay()` from `refresh_all()`.

**I. Config API** (`backend/app/api/v1/config.py`):
- Remove `"poll_interval_sports_api": "sports_api"` from `INTERVAL_WORKER_KEYS`.

**J. Seed** (`backend/app/seed.py`):
- Remove `"poll_interval_sports_api"` from `INTERVAL_DEFAULTS`.
- Remove `"poll_interval_sports_api_min"` from `INTERVAL_MINIMUMS`.
- Remove `"source_enabled_sports_api"` from `SOURCE_ENABLED_DEFAULTS`.

**K. All other workers that call compute_status_match / compute_is_critical / compute_is_flagged** — remove the `sports_api_status` argument:

- `backend/app/workers/ws_prophetx.py`: Two calls to `compute_status_match()` — remove `existing.sports_api_status` arg from both (around lines 155-162 and 249-256).
- `backend/app/workers/poll_prophetx.py`: All `compute_status_match()` calls — remove `existing.sports_api_status` or `event.sports_api_status` arg (4 call sites around lines 238, 257, 303, 320).
- `backend/app/workers/poll_odds_api.py`: `compute_status_match()` call — remove `best_match.sports_api_status` arg (line ~257).
- `backend/app/workers/poll_espn.py`: `compute_status_match()` call — remove `event.sports_api_status` arg (line ~294).
- `backend/app/workers/poll_oddsblaze.py`: `compute_status_match()` call — remove `best_match.sports_api_status` arg (line ~276).
- `backend/app/workers/poll_sports_data.py`: `compute_status_match()` calls — remove `px_event.sports_api_status` arg (lines ~651, 662). Also remove the line logging `sports_api_status=px_event.sports_api_status` (~line 670).
- `backend/app/workers/poll_critical_check.py`: `compute_is_critical()` call — remove `e.sports_api_status` arg (line ~44). Also remove `if event.sports_api_status: sources.append(...)` block from Slack message (lines ~72-73).

For `compute_is_flagged()`: `poll_sports_data.py` and `poll_sports_api.py` (deleted) are the callers. In `poll_sports_data.py`, update call to pass only `sdio_status`:
```python
compute_is_flagged(px_event.sdio_status)
```
(Previously it passed `(px_event.sdio_status, px_event.sports_api_status)`.)

**L. Tests**:

- `backend/tests/test_health.py`: Remove `"poll_sports_api"` from the expected worker keys (line ~36).
- `backend/tests/test_status_authority.py`: Remove `existing.sports_api_status = None` lines (lines ~108, 144, 178, 500). Update any `compute_status_match()` calls in test to match new 5-param signature.
- `backend/tests/test_ws_upsert.py`: Remove `existing.sports_api_status = None` line (~line 90). Update any `compute_status_match()` calls to match new signature.
  </action>
  <verify>
    <automated>cd /Users/doug/OpsMonitoringDash && ! grep -r "sports_api" backend/app/ --include="*.py" -l | grep -v __pycache__ | grep -v ".pyc" && ! grep -r "sports_api" backend/tests/ --include="*.py" | grep -v __pycache__ && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "sports_api" backend/app/ --include="*.py"` returns zero matches (excluding __pycache__)
    - `grep -r "sports_api" backend/tests/ --include="*.py"` returns zero matches
    - backend/app/monitoring/mismatch_detector.py compute_status_match has signature `(px_status, odds_api_status, sdio_status, espn_status, oddsblaze_status)`
    - backend/app/monitoring/mismatch_detector.py compute_is_critical has signature `(px_status, odds_api_status, sdio_status, espn_status, oddsblaze_status)`
    - backend/app/monitoring/mismatch_detector.py compute_is_flagged has signature `(sdio_status)`
    - backend/app/workers/celery_app.py does NOT contain `poll_sports_api`
    - backend/app/api/v1/health.py does NOT contain `poll_sports_api`
    - backend/app/api/v1/usage.py does NOT contain `poll_sports_api` or `SPORTS_API_FAMILIES`
    - backend/app/seed.py does NOT contain `sports_api`
    - backend/tests/test_health.py does NOT contain `poll_sports_api`
    - `cd /Users/doug/OpsMonitoringDash/backend && python -m pytest tests/ -x --tb=short` exits 0
  </acceptance_criteria>
  <done>All backend Python files purged of Sports API references; mismatch functions updated to 4-source signatures; all tests pass</done>
</task>

</tasks>

<verification>
1. `grep -r "sports_api" backend/app/ --include="*.py" | grep -v __pycache__` returns empty
2. `grep -r "sports_api" backend/tests/ --include="*.py"` returns empty
3. `grep -r "SPORTS_API" backend/app/ --include="*.py" | grep -v __pycache__` returns empty
4. `python -m pytest backend/tests/ -x --tb=short` passes
5. Migration file 009 exists with drop_column
</verification>

<success_criteria>
- Zero occurrences of "sports_api" in backend/app/**/*.py and backend/tests/**/*.py
- Alembic migration 009 drops the sports_api_status column
- All existing tests pass with updated function signatures
- compute_status_match, compute_is_critical, compute_is_flagged have correct reduced signatures
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt/11-01-SUMMARY.md`
</output>
