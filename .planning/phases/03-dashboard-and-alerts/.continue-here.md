---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T05:02:40.435Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
Three-source status architecture fully operational: Odds API, Sports API, SDIO columns
showing in dashboard. College basketball now matching via mascot-stripping fuzzy match
and basketball_ncaab added to Odds API. Sports API URL bug fixed (v1 not v2).
</current_state>

<completed_work>

## This session (2026-02-27)

### send_alerts wired on status mismatch
- poll_odds_api.py calls send_alerts_task.delay() when status_match=False
- alert_type="status_mismatch", includes team names and both statuses in message

### Per-source status columns (committed 7dab025)
- Replaced single real_world_status with odds_api_status, sports_api_status, sdio_status
- Migrations 003 (add/migrate) + 004 (rename api_football → sports_api)
- New Sports API client (api-sports.io) + poll_sports_api worker (30 min schedule)
- SPORTS_API_KEY configured with real key (42ecca70cc0638a9abedc9bb41c1de88)
- shared compute_status_match() in mismatch_detector.py

### Sports API URL fix
- v2.basketball/hockey/baseball.api-sports.io don't exist — correct is v1.*
- Fixed in backend/app/clients/sports_api.py

### College basketball matching
- Added basketball_ncaab to Odds API SPORT_KEY_MAP (was only basketball_nba)
- Added _strip_mascot() + _best_similarity() to poll_sports_api.py
  - ProphetX uses full mascot names ("Michigan Wolverines"), Sports API uses short ("Michigan")
  - Strips known mascot words before fuzzy compare when full name falls below threshold
- Sports API match count: 57 → 87 events per poll

### Ice Hockey + American Football sport name mapping
- Added "ice hockey" and "american football" (with spaces) to PX_TO_API_SPORTS
  - ProphetX uses these names; our map only had "hockey" and "american-football"

</completed_work>

<remaining_work>

## Still open

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

### SportsDataIO placeholder key
- SPORTSDATAIO_API_KEY=your-sportsdataio-api-key still a placeholder
- sdio_status column always null until real key provided

### Unmatched ProphetX events
- Golf, Tennis, MMA have no coverage in any external API
- Some college games still unmatched (big conference games not in Sports API free tier)
- Odds API NCAAB will fill some gaps on next 10-min poll

### ProphetX production URL
- Still on sandbox (api-ss-sandbox.betprophet.co)
- Production URL needed for real event/market data

</remaining_work>

<decisions_made>

- Sports API URLs are v1.* not v2.* for basketball/hockey/baseball (confirmed via DNS)
- Mascot stripping uses suffix matching against known mascot word list — not a DB lookup
- basketball_ncaab added to Odds API (not Sports API) since Odds API uses full team names
- "Ice Hockey" and "American Football" added to PX_TO_API_SPORTS with spaces (ProphetX naming)

</decisions_made>

<blockers>

- SDIO key missing — sdio_status always null
- ProphetX sandbox limits event variety (mostly test/Golf events)
- No remote repo — local-only git history

</blockers>

<context>
The three-column status architecture is working well. Lakers vs Suns correctly showing
FT in Sports API while ProphetX still shows live — exactly the mismatch detection use case.
College basketball matching improved significantly with mascot stripping.
Main remaining gap is SDIO (no key) and Golf/Tennis/MMA (no external API coverage).
</context>

<next_action>
Check if Odds API NCAAB poll has fired and populated odds_api_status for college basketball
games. Then either provide SDIO key or investigate Golf/Tennis coverage options.
</next_action>
