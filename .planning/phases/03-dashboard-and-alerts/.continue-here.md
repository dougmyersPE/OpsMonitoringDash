---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: done
last_updated: 2026-02-27T22:35:00.000Z
---

<current_state>
ProphetX WebSocket consumer (ws-consumer Docker service) is LIVE and connected.
Authenticated to ProphetX → fetched Pusher config (cluster=mt1, key=c20fa36fbc3c3c308ffa)
→ connected → subscribed to private-broadcast-service=3-device_type=5.

poll_prophetx reduced to 5-minute reconciliation interval.
</current_state>

<completed_work>

## This session (committed)
- Added `pysher>=1.0` to backend/pyproject.toml + regenerated uv.lock
- Created backend/app/workers/ws_prophetx.py (standalone Pusher consumer)
- Added ws-consumer service to docker-compose.yml (restart: unless-stopped)
- Reduced POLL_INTERVAL_PROPHETX default: 30 → 300 (reconciliation only)
- Deployed and confirmed connected: ws-consumer logs show ws_prophetx_subscribed

## Remaining open questions
1. sport_event messages: No events have arrived yet (no live games?) — need to
   verify the broadcast channel actually delivers sport_event pushes, vs needing
   per-tournament subscriptions (channel.bind("tournament_{id}", handler))
2. health_check event name: ProphetX may use a different event name on the channel;
   heartbeat is also written every 10s in the main loop so this is non-critical

</completed_work>

<remaining_work>

## Verify sport_event delivery
When ProphetX has live/upcoming events, monitor ws-consumer logs for:
  - ws_prophetx_event_created / ws_prophetx_event_updated log lines
  - If nothing arrives, try binding additional event names on the channel or
    subscribe to private channel (private-service=3-device_type=5-user={partnerId})

## Per-tournament subscriptions (if broadcast insufficient)
If broadcast channel doesn't deliver all sport_event updates:
  - Fetch active tournament IDs from DB
  - Subscribe to tournament_{id} channel for each
  - Re-subscribe when new tournaments appear (poll DB every N minutes)

</remaining_work>

<decisions_made>

- Pusher config endpoint response is flat (no "data" wrapper): {app_id, cluster, key, service}
- Broadcast channel name confirmed: private-broadcast-service=3-device_type=5
- Token TTL ~19 min (1139s) — ws_prophetx reconnects before expiry
- ws-consumer uses restart: unless-stopped (survives container crashes)

</decisions_made>

<blockers>

- sport_event delivery unconfirmed — no live events to test against yet
- health_check event name unconfirmed (non-critical, heartbeat written every 10s anyway)

</blockers>

<context>
ws-consumer is a standalone Docker service running python -m app.workers.ws_prophetx.
It connects to ProphetX Pusher, subscribes to broadcast channel, and handles:
  - sport_event → upsert Event in DB + publish SSE update
  - health_check → write Redis heartbeat (worker:heartbeat:ws_prophetx)

poll_prophetx still runs every 5 minutes as reconciliation fallback.
</context>

<next_action>
Monitor ws-consumer logs when ProphetX has live events to confirm sport_event delivery.
If no events arrive on broadcast channel, investigate per-tournament subscriptions.
</next_action>
