---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T10:27:21.331Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
ESPN coverage added for Golf/Tennis/MMA. Nginx upstream IP caching bug fixed
(dashboard no longer breaks on container restarts).
</current_state>

<completed_work>

## Previous session (2026-02-26)
- send_alerts wired on status mismatch
- Per-source status columns: odds_api_status, sports_api_status, sdio_status
- Sports API URL fix (v1 not v2), NCAAB added, mascot-stripping fuzzy match
- Ice Hockey + American Football sport name mapping

## This session (2026-02-27)
- ESPN client (espn_api.py) — unofficial API, no key, covers Golf/Tennis/MMA
- poll_espn worker — every 10 min, golf matches all market variants per tournament
  via substring check, tennis/MMA via competitor name fuzzy match + date window
- Migration 005: espn_status column
- espn_status wired into compute_status_match (pre/in/post canonical)
- espn_status in EventResponse schema, EventRow type, EventsTable column
- Fixed nginx: upstream block cached IP at startup → set $frontend variable
  forces DNS re-resolution on every request via Docker resolver 127.0.0.11
- Committed as ea9bfec

</completed_work>

<remaining_work>

## Still open

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

### SportsDataIO placeholder key
- SPORTSDATAIO_API_KEY=your-sportsdataio-api-key still a placeholder
- sdio_status column always null until real key provided

### ProphetX production URL
- Still on sandbox (api-ss-sandbox.betprophet.co)
- Production URL needed for real event/market data

### Unmatched ProphetX events
- MMA fights not matching yet — ProphetX sandbox doesn't have fighter names
  in home_team/away_team for those events; will auto-match on production
- Golf: Cognizant Classic matched (2 market variants). South African Championship
  not matched (different date/tournament in ESPN PGA scope)
- Tennis: ATP/WTA returned 0 events this poll (no active tournament right now)

</remaining_work>

<decisions_made>

- ESPN unofficial API chosen over paid alternatives (BALLDONTLIE, api-tennis.com)
  — free, no quota, same pre/in/post status pattern across all sports
- Golf matching uses substring check (ESPN name inside PX name) to handle ProphetX
  pattern of "2026 {Tournament} - {Market Type}" — updates all market variants
- Nginx fix: set $var approach forces Docker DNS re-resolution; upstream block
  was caching IP at nginx startup causing 502s after any container restart

</decisions_made>

<blockers>

- SDIO key missing — sdio_status always null
- ProphetX sandbox limits event variety (mostly test/Golf events)
- No remote repo — local-only git history

</blockers>

<context>
Three-source status architecture (Odds API, Sports API, ESPN) now fully operational.
Cognizant Classic correctly showing espn_status="in", prophetx_status="live",
status_match=true. Dashboard reliably loads after container restarts.
Main remaining gap is SDIO (no key) and switching ProphetX from sandbox to production.
</context>

<next_action>
Either provide SDIO API key (populates sdio_status) or provide ProphetX production
URL to switch from sandbox and see real event/market data including MMA/Tennis fighters.
</next_action>
