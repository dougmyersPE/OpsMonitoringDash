---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T23:25:00.000Z
---

<current_state>
ws-consumer is LIVE and handling events correctly. Pending first confirmed
DB write from a sport_event push (no sport_event messages have arrived since
23:13 restart after payload fix). poll_prophetx (5-min reconciliation) IS
writing to DB correctly.
</current_state>

<completed_work>

## This session (committed — 2 commits)

### commit 8b3e5d8: feat: add ProphetX WebSocket consumer (Pusher)
- Added pysher>=1.0 to backend/pyproject.toml + regenerated uv.lock
- Created backend/app/workers/ws_prophetx.py (standalone Pusher consumer)
- Added ws-consumer service to docker-compose.yml (restart: unless-stopped)
- Reduced POLL_INTERVAL_PROPHETX default: 30 → 300 (reconciliation only)

### commit 4b21cae: fix: normalize WS sport_event payload structure
- WS payload wraps event fields under "info": {"id": "...", "info": {...}, "tournament_id": "..."}
- For op=d: info.event_id is 0; real ID is top-level "id"
- Flatten info + set event_id before passing to _upsert_event

## Key discoveries
- Pusher config endpoint returns flat response (no "data" wrapper)
- Broadcast channel events arrive as tournament_{id}, NOT "sport_event"
- change_type field routes: sport_event / market / market_line / market_selections / sport_event_competitor
- Named channel.bind() won't match wildcard event names; patched channel._handle_event instead
- pysher logs raw WS messages via stdlib logging (invisible until basicConfig added)
- sport_event payloads confirmed: op=u has full data in info{}; op=d has empty info{}

</completed_work>

<remaining_work>

## Verify ws-consumer DB write (first priority next session)
Check ws-consumer logs for ws_prophetx_event_created / ws_prophetx_event_updated.
A sport_event message will come through next time ProphetX pushes an update.
Verify the event in DB has a unique (non-batch) last_prophetx_poll timestamp.

## Known change_types on broadcast channel (for reference)
- sport_event (op: c/u/d) → handled: upsert Event + SSE publish
- sport_event_competitor (op: u) → ignored (competitor updates)
- market (op: c/u) → ignored (market creation/update)
- market_line (op: c/u) → ignored (odds line update)
- market_selections (op: u) → ignored (liquidity/stake update)

## Optional cleanup
- Remove pysher basicConfig logging from ws_prophetx.py once confirmed working
  (or reduce to WARNING to suppress Connection: Message - ... noise)

</remaining_work>

<decisions_made>
- Pusher config endpoint: flat {app_id, cluster, key, service} — no "data" wrapper
- Broadcast channel: private-broadcast-service=3-device_type=5
- Event routing: by change_type field, not by Pusher event name
- Payload normalization: flatten info{} + derive event_id from top-level id for op=d
- ws-consumer restart policy: unless-stopped
</decisions_made>

<blockers>
None — ws-consumer is running and waiting for next sport_event push
</blockers>

<context>
ws-consumer runs as python -m app.workers.ws_prophetx in Docker.
poll_prophetx still runs every 5 minutes as reconciliation fallback.
Both write via _upsert_event → SyncSessionLocal → publish SSE.
</context>

<next_action>
Start next session by checking ws-consumer logs for ws_prophetx_event_updated
to confirm end-to-end DB write from WebSocket push.
</next_action>
