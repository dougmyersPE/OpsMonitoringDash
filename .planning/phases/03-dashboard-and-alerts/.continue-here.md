---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-26T22:03:23.052Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
status_match detection is now wired end-to-end: poll_odds_api computes it on
every real_world_status update. Dashboard red-row highlight will trigger
automatically when ProphetX and real-world statuses diverge tonight.
</current_state>

<completed_work>

## This session (second half)

### NHL matching gap investigation
- Confirmed 8 "misses" are correct behavior — completed games ProphetX already settled/removed
- FUZZY_THRESHOLD=0.80 correctly prevents false positives (e.g., "Tampa Bay vs Toronto" Odds API
  game from Feb 24 being mismatched onto tonight's "Florida Panthers vs Toronto" ProphetX event)
- No fix needed

### Committed: feat(poll_odds_api): compute status_match (1938b38)
- Added _PX_CANONICAL and _RW_CANONICAL dicts mapping to shared "scheduled/inprogress/final"
- _statuses_match() returns True if unknown (can't flag without both), False on disagreement
- Sets best_match.status_match after every real_world_status update
- 0 mismatches now (all not_started ↔ Scheduled agree)
- Will trigger red rows tonight when games finish and ProphetX is slow to settle

</completed_work>

<remaining_work>

## What's left

### status_match → alert pipeline not fully wired
- status_match=False sets the red row in the dashboard (already works via frontend)
- But the send_alerts task is NOT called when status_match flips to False
- poll_prophetx.py calls send_alerts on liquidity breaches, but not status mismatches
- Need to add: when status_match flips False in poll_odds_api, call send_alerts_task.delay()

### Spring Training baseball gap
- Odds API returns 0 MLB games during Spring Training
- 11 live Spring Training games show real_world_status=None
- Fix: real SportsDataIO API key (worker already exists)

### is_flagged not set automatically
- is_flagged on Event model is manually set via sync endpoint
- Could auto-set when status_match=False persists for N polls

</remaining_work>

<decisions_made>

- status_match uses canonical mapping (not string equality) — ProphetX "not_started" ≠ Odds API "Scheduled" as strings but they're the same state
- True (match) when either status is None — avoids false alarms for events not yet covered by Odds API
- ProphetX "suspended" maps to "inprogress" — suspended markets are still considered live

</decisions_made>

<blockers>

- SportsDataIO placeholder API key — Spring Training has no real_world_status
- ProphetX sandbox URL — production URL needed for real data
- send_alerts not called on status mismatch yet (next task)

</blockers>

<context>
The dashboard now has full end-to-end: ProphetX polls every 30s, Odds API every 10 min,
status_match computed on each Odds API update, red rows shown in UI when mismatch.
Tonight's NBA games (7pm ET+) are the first real test — when they go Final on Odds API
before ProphetX settles them, mismatches should appear.
The one missing link: mismatches don't trigger alerts yet, only visual highlighting.
</context>

<next_action>
In poll_odds_api.py, after setting status_match=False, call send_alerts_task.delay() with
alert_type="status_mismatch", entity_type="event", entity_id=str(best_match.id).
Import send_alerts from app.workers.send_alerts (same pattern as poll_prophetx.py).
</next_action>
