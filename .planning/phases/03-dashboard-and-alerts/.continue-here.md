---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T11:16:02.154Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
SDIO NCAAB matching now fully operational — 77 confirmed matches, sdio_status
populating correctly, status_match resolving accurately.
</current_state>

<completed_work>

## Previous sessions (2026-02-26 through 2026-02-27 morning)
- send_alerts wired on status mismatch
- Per-source status columns: odds_api_status, sports_api_status, sdio_status
- Sports API URL fix (v1 not v2), NCAAB added, mascot-stripping fuzzy match
- Ice Hockey + American Football sport name mapping
- ESPN client (espn_api.py) — unofficial API, covers Golf/Tennis/MMA
- poll_espn worker — espn_status column + compute_status_match wired
- Fixed nginx: upstream block cached IP → set $frontend forces DNS re-resolution

## This session (2026-02-27)
- **PX Event ID column on dashboard** — added prophetx_event_id to EventRow type
  and EventsTable as first column (monospace font)
- **SDIO API key wired** — 0c29f72f... set in .env for NCAAB only
- **SDIO CBB URL fix** — SPORT_PATH_MAP {"ncaab": "cbb"} in sportsdataio.py
  (was hitting /ncaab/ which 404'd; correct path is /cbb/)
- **ProphetX stale event fix** — poll_prophetx.py now marks events as "ended"
  when they drop off get_sport_events response (PX only returns active events).
  3h cutoff past scheduled_start. Batch of events including 10076927 corrected.
- **Empty string status bug** — compute_status_match was treating "" as a data
  source; changed guard from `is None` to `not source_status`
- **SDIO NCAAB matching pipeline fixed** — 4 bugs combined to produce 0 matches:
  1. Sport name: "ncaab" vs ProphetX "Basketball" → added SDIO_TO_PX_SPORT map
  2. Team abbreviations (TROY, LAMON): added get_team_names() via /cbb/scores/json/Teams
     (1321 teams), resolves to "Troy Trojans" etc. before fuzzy matching
  3. Wrong timestamp field: DateTime (local ET) vs DateTimeUTC (UTC) → swapped
     priority to DateTimeUTC; was causing 5h offset → time score = 0.0
  4. "upcoming" missing from _PX_CANONICAL → added "upcoming": "scheduled"
- **Result**: 100 matched games, 77 confirmed (confidence=1.0), sdio_status
  populating, ended/Final + ended/F/OT + not_started/Scheduled all status_match=true

</completed_work>

<remaining_work>

## Still open / not started

### poll_sports_api.py missing update_event_status call
- Sports API detects mismatches but only sends Slack alert, never calls
  update_event_status.delay() — the SDIO worker does call it
- Should add the same enqueue pattern when mismatch is detected

### ProphetX API write endpoint
- update_event_status.py is still stubbed (logs intent, updates local DB only)
- ProphetX docs reviewed: get_sport_events returns current status per event
  but no write/update endpoint was found in the Market Maker API
- User said "entirely incorrect" when I concluded no write endpoint exists
  — may need to dig deeper into B2B API or ask Doug for the endpoint

### SDIO key placeholder comment in code
- Comment in poll_sports_data.py still says "extend after confirming coverage"
  — accurate but could be clearer about current NCAAB-only state

### SportsDataIO NFL/Soccer/NHL/NBA coverage
- Current key is NCAAB-only per user instruction
- SUPPORTED_SPORTS = ["ncaab"] only

### ProphetX production URL
- Still on sandbox (api-ss-sandbox.betprophet.co)
- Production URL needed for real event/market data

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

</remaining_work>

<decisions_made>

- SDIO sport tag "ncaab" mapped to "basketball" (ProphetX convention) at normalization
  time, not in EventMatcher — avoids touching EventMatcher's general logic
- get_team_names() called once per poll cycle (not cached) — teams endpoint is fast
  and teams don't change mid-season; acceptable overhead
- DateTimeUTC preferred over DateTime for SDIO CBB games — SDIO local times are ET
  which causes 5h UTC offset killing the time-confidence score
- "upcoming" added to _PX_CANONICAL as "scheduled" — confirmed ProphetX uses
  "upcoming" not "not_started" for pre-game events
- Stale event sweep uses 3h cutoff — conservative to avoid marking long-running
  live events as ended; covers games that typically run 2-2.5h max

</decisions_made>

<blockers>

- ProphetX write endpoint unknown — update_event_status can't push to ProphetX
- SDIO key is NCAAB only — sdio_status null for Baseball/Soccer/Hockey/NBA events

</blockers>

<context>
Three data pipelines (Odds API, Sports API, ESPN, SDIO) now all functioning.
Dashboard shows PX Event ID. Status matching logic is solid across all sources.
The remaining gap is the ProphetX write path (can we push status updates?)
and expanding SDIO coverage to other sports once Doug provides keys.
</context>

<next_action>
Investigate ProphetX B2B API or ask Doug directly: is there an endpoint to
push event status updates to ProphetX? The MM API doesn't have one visible
in the docs but Doug indicated get_sport_events shows current status, suggesting
the system is supposed to READ status from PX, not WRITE to it.
</next_action>
