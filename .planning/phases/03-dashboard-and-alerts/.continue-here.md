---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T19:13:22.188Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
Zero mismatches. All data pipelines polling yesterday/today/tomorrow.
</current_state>

<completed_work>

## Previous sessions (2026-02-26 through 2026-02-27 afternoon)
- send_alerts wired on status mismatch
- Per-source status columns: odds_api_status, sports_api_status, sdio_status
- Sports API URL fix (v1 not v2), NCAAB added, mascot-stripping fuzzy match
- Ice Hockey + American Football sport name mapping
- ESPN client (espn_api.py) — unofficial API, covers Golf/Tennis/MMA
- poll_espn worker — espn_status column + compute_status_match wired
- Fixed nginx: upstream block cached IP → set $frontend forces DNS re-resolution
- PX Event ID column on dashboard
- SDIO NCAAB matching pipeline fixed (4 bugs)
- ProphetX stale event fix — marks events "ended" when dropped from API response
- Sports API matching overhaul — prefix match + token containment strategies
- SDIO_TO_PX_STATUS["Scheduled"] = "not_started" bug fix
- Dashboard filters added (toggle buttons)
- Poll intervals configurable via POLL_INTERVAL_* env vars (config.py + .env)
- Dashboard filters → dropdowns (Status, Sport); Mismatch/Flagged stay as toggle buttons
- Health indicators for all 5 APIs in SystemHealth.tsx (uses mget in health.py)
- ESPN expanded to all major sports: NBA, NFL, MLB, NHL, NCAAB, NCAAF, MLS, EPL,
  La Liga, Bundesliga, Serie A, Ligue 1, UCL, UEL — 46+ events matching per cycle
- Status display normalized via frontend/src/lib/statusDisplay.ts — all columns
  show Not Started / Live / Ended; raw DB values and mismatch logic unchanged
- Scheduled start column added to EventsTable (between Sport and ProphetX Status)
- Full dark theme redesign — zinc-950/900 base, indigo-500 accent
- Drag-to-reorder group sort (GroupOrderControl chips)
- Not Listed display (italic zinc-600)

## Session 2026-02-27 evening (commits 12ce68c, 40f5e17, eee585d)

### Dynamic flag detection (12ce68c)
- `is_flagged` now derived from current source columns each poll cycle via `compute_is_flagged()`
- Clears automatically when no source reports a flag-worthy status — no manual clearing needed
- Sports API flag statuses added: CANC, PSP, SUSP, ABD
- Source status columns now use distinct colors: emerald (Live), sky-400 (Not Started),
  zinc-400 (Ended), amber (flag-worthy raw values like Canceled)

### False-positive mismatch fixes (40f5e17)
Three root causes eliminated — went from 23 false mismatches to 0:
1. poll_prophetx now recomputes status_match for ALL DB events each cycle
2. poll_sports_data mismatch detection gated on `is_confirmed` only
3. poll_sports_api: 24h date-distance guard rejects cross-day wrong matches

### Tomorrow's games coverage (eee585d)
- ESPN and Sports API fetch yesterday, today, AND tomorrow (3 days)

## Session 2026-02-27 evening continued (this session — uncommitted)

### SDIO team names caching
- poll_sports_data.py: get_team_names() was called every 30s (every poll cycle)
- Fixed: team names now cached in Redis with 24h TTL (key: sdio:team_names:{sport})
- Reduces SDIO API calls from 2,880/day → 1-2/day for the Teams endpoint
- Works across all Celery fork worker processes (module-level dict would not)

### Column sorting on EventsTable
- All table headers are now clickable to sort (ChevronUp/Down indicators)
- String columns: A→Z / Z→A
- Date columns (Starts, Checked): oldest→newest / newest→oldest
- Status columns: sort by normalized display value (Not Started, Live, Ended)
- Flag column: flagged rows first on ascending
- Nulls always sort last regardless of direction
- Active column sort bypasses the group-order (Live/Not Started/Ended) bucketing
- Inactive headers show faint ⇅ on hover

### Clear Filters button
- Added to EventsTable section header — always visible, dimmed when nothing active
- Clears all dropdowns, toggles (Mismatch/Flagged), AND active column sort
- Replaces the old conditional inline "Clear" text button

</completed_work>

<remaining_work>

## Still open / not started

### Schools with different naming conventions (Sports API vs ProphetX)
- CSUN, UNCG, UNCW, SIUE, UMass Lowell, UC San Diego, Kansas City Roos
- 7 schools with genuinely different names across providers — deferred

### SDIO key coverage
- Current key is NCAAB only (SUPPORTED_SPORTS = ["ncaab"])
- Other sports waiting on Doug for additional SDIO subscription keys

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

### Odds API out of credits
- Free tier exhausted — no odds_api_status data until credits reset or paid plan

</remaining_work>

<decisions_made>

- SDIO team names cached in Redis (not module-level dict) — works across fork workers
- 24h TTL on team names cache — teams don't change mid-season
- Column sort bypasses group-order bucketing when active — flat global sort takes over
- Clear Filters button lives in section header (always visible) rather than inline/conditional
- `hasActiveFilters` now includes `!!sortCol` so the button activates when a column sort is set

</decisions_made>

<blockers>

- SDIO key is NCAAB only — sdio_status null for Baseball/Soccer/Hockey/NBA events
- No remote git repo — all commits local only
- Odds API free tier exhausted — odds_api_status won't update until credits reset

</blockers>

<context>
System is fully operational. Zero mismatches. Five data pipelines polling.
This session focused on production readiness: API load analysis, SDIO call reduction,
and UX improvements (column sorting, clear filters button).
Current changes (SDIO cache + column sort + clear filters) are built and running
in Docker but NOT yet committed to git.
</context>

<next_action>
Commit the three changes from this session:
1. SDIO team names Redis cache (poll_sports_data.py)
2. Column sorting (EventsTable.tsx)
3. Clear Filters button (EventsTable.tsx)
Then ask Doug what's next.
</next_action>
