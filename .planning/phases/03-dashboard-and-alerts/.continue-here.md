---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-26T21:32:53.549Z
---

<current_state>
All 3 phases complete. Post-phase operational work continues. Stack is live and healthy.
This session implemented The Odds API as a third real-world data source, fixed multiple
worker bugs, fixed nginx DNS caching, and resolved real_world_status matching issues.
</current_state>

<completed_work>

## This session

### Committed: fix(poll_prophetx): fix markets fetch, events shape, sport field (06122d6)
Post-phase live debugging fixes that were uncommitted:
- get_markets_raw() called per-event with event_id (ProphetX requires it)
- Events unwrapped two levels: {"data": {"sport_events": [...]}}
- event_id injected into each market object
- sport column checks sport_name field

### Committed: feat(poll_odds_api): Add The Odds API as third data source (ea0c683)
- `backend/app/clients/odds_api.py` — GET /v4/sports/{sport_key}/scores/
- `backend/app/workers/poll_odds_api.py` — runs every 10 min (600s RedBeat)
- Fuzzy team name matching (SequenceMatcher, threshold 0.80) with sport+date index
- Updates real_world_status and last_real_world_poll on matched events
- Derives: Final (completed), InProgress (past commence_time), Scheduled
- ODDS_API_KEY=718be09f76addce4b12944f03c9be0d6 added to .env and config.py
- Beat registered: redbeat:poll-odds-api, every 600s, confirmed in Redis

### Committed: fix(prophetx+nginx): competitors array + nginx DNS caching (f2c4d93)
- poll_prophetx: team names are in competitors[].name with side:"home"/"away" — was None for all 83 Basketball events. Also added "scheduled" to start time lookup.
- nginx: resolver 127.0.0.11 valid=10s + set $backend variable forces per-request DNS resolution. Prevents 502 after any backend rebuild.
- After fix: 60/148 NBA/NHL/MMA/Soccer events matched at 1.0 score

### Committed: fix(workers): name fallback + days_from=3 (416d0eb)
- poll_prophetx: parse "Away at Home" from event name when competitors array is empty (Spring Training games)
- poll_odds_api: days_from 1→3 catches games up to 3 days old (marks Final correctly)
- One-time DB backfill: 5 stale no-team baseball events had team names written from name field

</completed_work>

<remaining_work>

## Known gaps / next work

### Spring Training baseball — no real_world_status possible
- Odds API returns 0 games for baseball_mlb during Spring Training (no betting lines)
- 11 live Spring Training games (Red Sox, Dodgers, etc.) will show real_world_status=None until regular season starts March 26
- Fix: get a real SportsDataIO API key — that worker already exists and covers Spring Training
- Regular season MLB (March 26+) will work automatically via Odds API

### Dashboard doesn't render real_world_status / status mismatch
- real_world_status is populated in DB for 60 events (NBA/NHL/MMA/Soccer/MLS)
- Frontend EventRow likely doesn't display it yet (wasn't part of Phase 3 plans)
- Status mismatch detection (prophetx_status ≠ real_world_status) not implemented yet
- This is the core monitoring purpose — events diverging between ProphetX and real world

### NHL/Basketball unmatched events
- Some NHL teams score 0.6-0.7 (below 0.80 threshold) — likely different team name formats
  e.g., "Buffalo Sabres" vs slightly different name in ProphetX
- Could lower threshold cautiously OR inspect specific mismatches

### MMA unmatched (best_score=0.0)
- MMA Odds API returns future speculative fights that don't exist in ProphetX yet
- Expected behavior — not a bug

</remaining_work>

<decisions_made>

- The Odds API over API-Sports: API-Sports free plan hard-blocks current season data
- days_from=3: captures games settled up to 3 days ago (no extra API credits — same number of requests)
- FUZZY_THRESHOLD=0.80: works well for team names; MMA individual fighter names need lower threshold potentially
- nginx resolver fix: use Docker's 127.0.0.11 DNS + variable in proxy_pass (not upstream block) — this is the correct long-term pattern
- Spring Training gap accepted: not worth building a third data source just for 6 weeks of preseason
- DB backfill for stale no-team events: simpler than replaying old polls

</decisions_made>

<blockers>

- SportsDataIO 401: placeholder API key in .env — get real key from Doug for Spring Training + MLB coverage
- ProphetX real URL still sandbox (api-ss-sandbox.betprophet.co) — production URL needed for real ProphetX data
- Some NHL events unmatched (score 0.6-0.7) — not blocking but reduces coverage

</blockers>

<context>
Stack is healthy: 5 containers running, ProphetX polling every 30s (173+ events, 9800+ markets),
Odds API polling every 10 min (60 events getting real_world_status). The core data pipeline
works end-to-end. The next logical feature is showing status mismatches prominently in the
dashboard — when prophetx_status says "live" but Odds API says "Final", that's a real alert.
The real_world_status column is in the DB and being populated; it just needs to be surfaced in the UI.
</context>

<next_action>
Start with: Check if the frontend EventRow component renders real_world_status, and if not,
add it. Then implement the status mismatch highlight — any event where real_world_status and
prophetx_status conflict should be flagged visually and trigger the existing send_alerts pipeline.
</next_action>
