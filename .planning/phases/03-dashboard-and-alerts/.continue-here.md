---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T11:53:40.354Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
Dashboard filters added. Sports API matching improved. Critical mismatch bug fixed.
</current_state>

<completed_work>

## Previous sessions (2026-02-26 through 2026-02-27 morning)
- send_alerts wired on status mismatch
- Per-source status columns: odds_api_status, sports_api_status, sdio_status
- Sports API URL fix (v1 not v2), NCAAB added, mascot-stripping fuzzy match
- Ice Hockey + American Football sport name mapping
- ESPN client (espn_api.py) — unofficial API, covers Golf/Tennis/MMA
- poll_espn worker — espn_status column + compute_status_match wired
- Fixed nginx: upstream block cached IP → set $frontend forces DNS re-resolution
- PX Event ID column on dashboard
- SDIO NCAAB matching pipeline fixed (4 bugs)
- ProphetX stale event fix — marks events "ended" when dropped from API response

## This session (2026-02-27 afternoon)
- **update_event_status wired into poll_sports_api.py** — when Sports API detects a
  mismatch, now enqueues update_event_status.delay() same as SDIO worker does
- **Sports API matching overhaul** — replaced hard-coded _SCHOOL_ALIASES dict and
  growing mascot list with two dynamic strategies:
  1. Prefix match: "Montana" prefix of "Montana Grizzlies" → 1.0
  2. Token containment: all tokens of api_name in db_name — naturally prevents
     same-mascot cross-matches ("Yale Bulldogs" won't match "Bryant Bulldogs")
  Added new mascot words for completeness but removed brittle alias map entirely.
  Result: 105 events updated per poll cycle (up from ~0 for most college teams)
- **SDIO_TO_PX_STATUS bug fix** — "Scheduled" was mapped to "upcoming" (UNCONFIRMED
  from project setup). Actual ProphetX value is "not_started". Was causing every
  scheduled event to be falsely flagged as a mismatch and trigger spurious
  update_event_status tasks. Fixed to "not_started".
- **Dashboard filters** — toggle buttons added to EventsTable:
  - Status filter (multi-select: live / not_started / ended)
  - Sport filter (multi-select: derived dynamically from event data)
  - Mismatches toggle (show only status_match=false)
  - Flagged toggle (show only is_flagged=true)
  - Clear filters link + "Showing X of Y events" count when active

</completed_work>

<remaining_work>

## Still open / not started

### Schools with different naming conventions (Sports API vs ProphetX)
- CSUN, UNCG, UNCW, SIUE, UMass Lowell, UC San Diego, Kansas City Roos
- These are 7 schools with genuinely different names across providers
- Not matching is safer than a wrong match; deferred until needed

### ProphetX API write endpoint
- update_event_status.py is still stubbed (logs intent, updates local DB only)
- Doug said "entirely incorrect" when told no write endpoint exists
- May need to dig deeper or ask Doug directly

### SDIO key coverage
- Current key is NCAAB only (SUPPORTED_SPORTS = ["ncaab"])
- Other sports waiting on Doug for additional SDIO subscription keys

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

</remaining_work>

<decisions_made>

- Token containment replaces alias map for Sports API matching — dynamic, safe
  against same-mascot false positives, no maintenance burden
- SDIO_TO_PX_STATUS["Scheduled"] = "not_started" (was "upcoming") — confirmed from
  prophetx_status_values_observed logs showing only not_started/live/ended
- "upcoming" kept in _PX_CANONICAL as alias in case ProphetX ever returns it
- Dashboard filters are client-side (no extra API calls) — all events already fetched

</decisions_made>

<blockers>

- ProphetX write endpoint unknown — update_event_status can't push to ProphetX
- SDIO key is NCAAB only — sdio_status null for Baseball/Soccer/Hockey/NBA events

</blockers>

<context>
System is operationally solid. Four data pipelines polling. Dashboard showing live
data with filters. The main outstanding question is whether ProphetX has a write
endpoint at all — if not, the monitor is read-only (detect and alert only).
</context>

<next_action>
Resume with: ask Doug about ProphetX write endpoint — does it exist? If yes, what
is the path/method? If no, update_event_status should be documented as alert-only.
Or: expand SDIO coverage once Doug provides additional subscription keys.
</next_action>
