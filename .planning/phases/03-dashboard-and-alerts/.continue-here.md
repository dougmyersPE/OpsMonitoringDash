---
phase: 03-dashboard-and-alerts
task: out-of-phase
total_tasks: n/a
status: in_progress
last_updated: 2026-02-27T12:44:14.713Z
---

<current_state>
All 3 phases complete. Post-phase operational work. Stack live and healthy.
Dashboard filters converted to dropdowns. All 5 API pollers shown in health bar.
ESPN expanded to all major sports. Status display normalized to ProphetX naming.
</current_state>

<completed_work>

## Previous sessions (2026-02-26 through 2026-02-27 morning)
- send_alerts wired on status mismatch
- Per-source status columns: odds_api_status, sports_api_status, sdio_status
- Sports API URL fix (v1 not v2), NCAAB added, mascot-stripping fuzzy match
- Ice Hockey + American Football sport name mapping
- ESPN client (espn_api.py) — unofficial API, covers Golf/Tennis/MMA
- poll_espn worker — espn_status column + compute_status_match wired
- Fixed nginx: upstream block cached IP → set $frontend forces DNS re-resolution
- PX Event ID column on dashboard
- SDIO NCAAB matching pipeline fixed (4 bugs)
- ProphetX stale event fix — marks events "ended" when dropped from API response
- Sports API matching overhaul — prefix match + token containment strategies
- SDIO_TO_PX_STATUS["Scheduled"] = "not_started" bug fix
- Dashboard filters added (toggle buttons)

## This session (2026-02-27 afternoon)
- **ProphetX write endpoint** — confirmed no write endpoint exists; update_event_status
  remains as alert/log-only. No code changes needed.
- **Poll intervals configurable** — 5 POLL_INTERVAL_* env vars added to config.py
  and .env (PROPHETX=30, SPORTS_DATA=30, ODDS_API=600, SPORTS_API=1800, ESPN=600).
  Beat schedule reads from settings. Heartbeat TTLs scale to interval * 3.
- **Dashboard filters → dropdowns** — Status and Sport changed from toggle buttons
  to native <select> elements. Mismatch/Flagged remain as toggle buttons.
- **Health indicators for all 5 APIs** — health.py now checks all 5 heartbeat keys
  (uses mget). SystemHealth.tsx shows ProphetX, SDIO, Odds API, Sports API, ESPN.
- **ESPN expanded to all major sports** — Added NBA, NFL, MLB, NHL, NCAAB, NCAAF,
  MLS, EPL, La Liga, Bundesliga, Serie A, Ligue 1, UCL, UEL endpoints.
  PX_TO_ESPN updated with all sport name variants (basketball, ice hockey, etc.).
  Column was blank because old Golf/Tennis/MMA weren't matching; 46 events now
  updated per cycle at 1.0 match score.
- **Status display normalized** — Created frontend/src/lib/statusDisplay.ts with
  normalizeStatus() function mapping all raw API values (NS, FT, pre, post,
  Scheduled, Final, Q1…) to ProphetX-style labels (Not Started / Live / Ended).
  Applied to all 5 status columns and the status filter dropdown option labels.
  Raw DB values and mismatch logic unchanged.

</completed_work>

<remaining_work>

## Still open / not started

### Schools with different naming conventions (Sports API vs ProphetX)
- CSUN, UNCG, UNCW, SIUE, UMass Lowell, UC San Diego, Kansas City Roos
- 7 schools with genuinely different names across providers — deferred

### SDIO key coverage
- Current key is NCAAB only (SUPPORTED_SPORTS = ["ncaab"])
- Other sports waiting on Doug for additional SDIO subscription keys

### No remote git repository
- git push fails — no remote configured
- User decided not to set up GitHub for now

</remaining_work>

<decisions_made>

- ProphetX has no write endpoint — update_event_status stays as log/alert-only
- Poll intervals via env vars (not DB config) — simple, no UI needed
- Heartbeat TTL = interval * 3 — prevents false health failures on slight delays
- Filter dropdowns are single-select native <select> (not multi-select) — simpler
- Status normalization is frontend display-only — DB stores raw values, mismatch
  detection logic completely unchanged
- ESPN `POST` and `post` duplicate key resolved by keeping ESPN's lowercase `post`
  and removing Sports API's `POST` entry (both map to "Ended" anyway)

</decisions_made>

<blockers>

- SDIO key is NCAAB only — sdio_status null for Baseball/Soccer/Hockey/NBA events
- No remote git repo — all commits local only

</blockers>

<context>
System is fully operational. Five data pipelines polling. Dashboard clean with
uniform status naming and dropdown filters. Health bar shows all 5 pollers.
ESPN now covers all major sports and is actively matching events (46/cycle).
The main outstanding work is expanding SDIO coverage once Doug gets more keys.
</context>

<next_action>
Resume with: ask Doug what's next — possible SDIO key expansion, or new feature work.
</next_action>
