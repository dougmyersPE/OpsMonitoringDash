---
phase: 03-dashboard-and-alerts
task: post-deployment
total_tasks: n/a
status: in_progress
last_updated: 2026-03-01T22:31:09.653Z
---

<current_state>
All 3 phases complete. This session was about deploying the app to a Hetzner server
and making it accessible to the team via Tailscale. Deployment is live and working.
</current_state>

<completed_work>

- Deployed app to Hetzner CX23 VPS (46.225.233.32)
- Installed Docker + Tailscale on server; joined company Tailscale network
- Diagnosed and fixed Docker bridge networking conflict: company Tailscale routes
  172.17.0.0/16 via tailscale0, hijacking Docker's default bridge. Fixed by setting
  Docker default-address-pools to 192.168.0.0/16 in /etc/docker/daemon.json
- Set DNS to Hetzner servers (185.12.64.2) in Docker daemon.json
- Built all 5 images successfully and started full stack
- App is live at http://100.111.249.12 (Tailscale IP) — accessible to anyone on company Tailscale
- Pushed code to GitHub: https://github.com/dougmyersPE/OpsMonitoringDash (private)
- Removed .env from git history (had Slack webhook URL — GitHub push protection caught it)
- Added .gitignore to prevent future secret commits
- Paused Slack alerts: cleared SLACK_WEBHOOK_URL on server (.env) and ran docker compose up -d
- Stopped local Mac Docker stack (was running alongside server, causing duplicate alerts)

</completed_work>

<remaining_work>

- Re-enable Slack alerts when ready (set SLACK_WEBHOOK_URL in server .env, run docker compose up -d worker beat)
- Investigate false-positive critical alerts: Sports API returns Q1 for games ProphetX
  shows as not_started — likely Sports API matching wrong game. These are real critical
  alerts (ProphetX genuinely behind) but fire too frequently on busy game days
- Validate event ID matching confidence threshold (0.90) against real ProphetX data
- Consider whether status_match=false visibility in dashboard needs explanation for team

</remaining_work>

<decisions_made>

- Tailscale (not SSL/domain) for team access — company already uses it, zero friction
- Hetzner CX23 (~$6/mo) over DigitalOcean for cost
- Docker bridge subnet changed to 192.168.0.0/16 to avoid Tailscale route conflict
  (Tailscale was routing 172.17.0.0/16 via tailscale0 — company subnet advertisement)
- Paused alerts by clearing SLACK_WEBHOOK_URL (not by modifying code)
- Stopped local Docker stack — server is now sole running instance

</decisions_made>

<blockers>

- Critical alerts fire on startup when server comes up mid-game (ProphetX WS consumer
  takes ~5 min to catch up with live game states). This is expected behavior, not a bug.
- Sports API returning Q1 (live) for games ProphetX shows as not_started — causes
  legitimate-looking critical alerts but may be false positives from bad event matching

</blockers>

<context>
The app is fully deployed and running. Team can access at http://100.111.249.12 with
their company Tailscale VPN on. Login: doug.myers@betprophet.co / ProphetX2025!

Server: root@46.225.233.32 (or via Tailscale: root@100.111.249.12)
Code: /root/prophet-monitor on server, ~/Prophet API Monitoring locally
GitHub: https://github.com/dougmyersPE/OpsMonitoringDash

To deploy updates: rsync changes to server, docker compose build <svc>, docker compose up -d <svc>
</context>

<next_action>
Re-enable alerts when ready: SSH to server, edit ~/prophet-monitor/.env,
set SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REDACTED,
then run: cd ~/prophet-monitor && docker compose up -d worker beat
</next_action>
